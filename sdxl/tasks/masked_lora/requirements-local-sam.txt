# Dependencies for the local SAM stage (mask_SAM/*.py).
# PyTorch and segment-anything are installed separately; see the
# top-level README for the local SAM setup.
numpy>=1.23
Pillow>=10
opencv-python-headless>=4.5
